[pytest]
# -s / --capture=no lets the ChromaDB fixture read from stdin so the
# interactive Docker prompt works without needing to pass -s every time.
# Override per-run with --capture=fd if you want output suppressed.
addopts = -s
asyncio_mode = strict
